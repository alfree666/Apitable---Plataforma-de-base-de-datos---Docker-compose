version: '3.3'

services:

    apitable:
        image: apitable/all-in-one:latest
        container_name: apitable                
        volumes:
            - ./apitable:/apitable
        ports:
            - '8200:80'
        restart: unless-stopped    

